"""Tests for InMemoryStore — REMOVED.

InMemoryStore was removed from the package. The IngestBackend protocol
is now tested via test_client.py (using mock backends) and
test_backends.py (protocol conformance).
"""
